

## 理论模型


---

## 一、改进的模型设定

### （一）社会人口构成

社会人口按代际划分为三代：孙代（$N_{t+1}$）、子代（$N_t$）和父代（$N_{t-1}$）。

**定义：**
- 生育率：$n_t$（每个子代个体在t期生育的孩子数量）
- 存活概率：$p$（父代存活至t+1期的概率）
- 孙代人口：$N_{t+1} = n_t \cdot N_t$
- 父代人口：$N_{t-1} = N_t/n_{t-1}$

**家庭抚养比和赡养比：**
$$C_t = \frac{N_{t+1}}{N_t} = n_t \quad \text{(抚养比)}$$

$$D_t = \frac{N_{t-1}}{N_t} = \frac{1}{n_{t-1}} \quad \text{(赡养比)}$$

---

### （二）改进的人力资本积累函数

**原模型的问题：** 未明确区分子代和父代的时间投入参数。

**改进后的人力资本积累函数：**

$$h_{t+1} = A \cdot h_t^\alpha \cdot e_t^\beta \cdot (\theta_c \tau_c + \theta_g \tau_g)^\gamma \quad \text{(式1)}$$

其中：
- $h_t$：子代t期的人力资本水平
- $e_t$：孙代接受的教育投资（物质投入）
- $\tau_c$：子代对孙代的教育时间投入
- $\tau_g$：父代对孙代的隔代抚育时间投入
- $\theta_c$：子代时间投入的效率参数
- $\theta_g$：父代时间投入的效率参数
- $A, \alpha, \beta, \gamma$：技术参数，满足 $\alpha + \beta + \gamma = 1$（规模报酬不变）

**合理假设：** $\theta_c > \theta_g$（子代教育效率高于父代）

**理论依据：**
1. 代际时间投入存在不完全替代性
2. 子代与父代的教育能力存在差异
3. 反映隔代抚育在人力资本形成中的作用

---

### （三）子代的时间约束与劳动供给

子代拥有1单位时间，需在以下活动中分配：
- 抚养孙代所需时间：$n_t \cdot \tau_c$
- 照料父代所需时间：$\tau_o$
- 劳动时间：$l_t$

**时间约束：**
$$1 = n_t \tau_c + \tau_o + l_t \quad \text{(式2)}$$

考虑父代隔代抚育的替代效应，子代有效劳动时间为：
$$l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g \quad \text{(式3)}$$

其中：
- $\xi$：父代时间投入对子代时间的替代系数（$0 < \xi < 1$）
- $\tau_g$：父代隔代抚育时间

**说明：** 父代照料孙代可以部分替代子代的抚育时间，使子代有更多时间用于劳动。

---

### （四）预算约束

**1. 子代预算约束（t期）：**

$$(1-\tau)\omega_t h_t l_t = c_t + s_t + \tau_o \omega_t h_t l_t + n_t e_t - b_t \quad \text{(式4)}$$

其中：
- $(1-\tau)\omega_t h_t l_t$：税后劳动收入
  - $\omega_t$：单位人力资本工资率
  - $h_t$：子代人力资本水平
  - $l_t$：有效劳动时间
  - $\tau = \tau_p + \tau_e$：养老保险总缴费率（个人+单位）
- $c_t$：子代当期消费
- $s_t$：子代储蓄
- $\tau_o \omega_t h_t l_t$：对父代的赡养支出（占收入比例为$\tau_o$）
- $n_t e_t$：对孙代的教育投资
- $b_t$：继承收入

**2. 继承收入的构成（t期）：**

$$b_t = (1+r_t)s_{t-1}\frac{1-p}{n_{t-1}} + D_t m_{g,t} \quad \text{(式5)}$$

其中：
- $(1+r_t)s_{t-1}\frac{1-p}{n_{t-1}}$：上期去世父代的储蓄遗产
- $D_t m_{g,t}$：当期父代的隔代抚育物质转移

**3. 父代预算约束（t+1期）：**

$$c_{t+1}^o + n_t m_{g,t+1} = (1+r_{t+1})s_t + \tau_o \omega_{t+1} h_{t+1} l_{t+1} + pen_t \quad \text{(式6)}$$

其中：
- $c_{t+1}^o$：父代消费
- $n_t m_{g,t+1}$：父代对孙代的物质转移
- $(1+r_{t+1})s_t$：储蓄收益
- $\tau_o \omega_{t+1} h_{t+1} l_{t+1}$：子代赡养支出
- $pen_t$：养老金收入

---

### （五）效用函数

子代代表性个体的效用函数由四部分组成：

$$U_t = u(c_t) + \beta u(c_{t+1}) + \delta v(h_{t+1}) + \phi u(c_{t+1}^o) \quad \text{(式7)}$$

其中：
- $u(c_t)$：子代当期消费效用
- $\beta u(c_{t+1})$：子代未来消费效用（$\beta$为折现因子）
- $\delta v(h_{t+1})$：孙代人力资本积累带来的效用（$\delta$为子代对孙代教育的重视程度）
- $\phi u(c_{t+1}^o)$：父代消费效用（$\phi$为子代的利他程度）

**采用CRRA效用函数：**
$$u(c) = \frac{c^{1-\sigma}}{1-\sigma}, \quad v(h) = \frac{h^{1-\sigma}}{1-\sigma}$$

其中$\sigma$为相对风险厌恶系数。

---

### （六）生产部门

**生产函数（Cobb-Douglas形式）：**
$$Y_t = D K_t^\alpha (h_t N_t l_t)^{1-\alpha} \quad \text{(式8)}$$

其中：
- $D$：全要素生产率
- $K_t$：资本存量
- $\alpha$：资本产出弹性
- $h_t N_t l_t$：有效劳动投入

**完全竞争市场均衡条件：**

资本租金率：
$$r_t + 1 = \alpha D \left(\frac{K_t}{h_t N_t l_t}\right)^{\alpha-1} \quad \text{(式9)}$$

单位人力资本工资率：
$$\omega_t = (1-\alpha) D \left(\frac{K_t}{h_t N_t l_t}\right)^\alpha \quad \text{(式10)}$$

---

### （七）市场均衡

**资本市场均衡：**
$$K_{t+1} = N_t s_t \quad \text{(式11)}$$

**商品市场均衡：**
$$Y_t = N_t c_t + N_{t-1} c_t^o + N_t n_t e_t + N_{t-1} m_{g,t} + K_{t+1} \quad \text{(式12)}$$

---

## 二、最优化问题求解

### （一）子代的优化问题

子代个体在给定价格和人口结构下，选择最优的$\{c_t, c_{t+1}, s_t, e_t, \tau_c\}$以最大化效用。

**拉格朗日函数：**

$$\mathcal{L} = u(c_t) + \beta u(c_{t+1}) + \delta v(h_{t+1}) + \phi u(c_{t+1}^o)$$
$$+ \lambda_1[(1-\tau-\tau_o)\omega_t h_t l_t + b_t - c_t - s_t - n_t e_t]$$
$$+ \lambda_2[(1+r_{t+1})s_t + \tau_o \omega_{t+1} h_{t+1} l_{t+1} + pen_t - c_{t+1}^o - n_t m_{g,t+1}]$$
$$+ \lambda_3[A h_t^\alpha e_t^\beta (\theta_c \tau_c + \theta_g \tau_g)^\gamma - h_{t+1}]$$

---

### （二）一阶条件

**对$c_t$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial c_t} = u'(c_t) - \lambda_1 = 0$$
$$\Rightarrow \lambda_1 = c_t^{-\sigma} \quad \text{(式13)}$$

**对$c_{t+1}$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial c_{t+1}} = \beta u'(c_{t+1}) - \lambda_1 = 0$$
$$\Rightarrow \lambda_1 = \beta c_{t+1}^{-\sigma} \quad \text{(式14)}$$

**对$c_{t+1}^o$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial c_{t+1}^o} = \phi u'(c_{t+1}^o) - \lambda_2 = 0$$
$$\Rightarrow \lambda_2 = \phi (c_{t+1}^o)^{-\sigma} \quad \text{(式15)}$$

**对$s_t$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial s_t} = -\lambda_1 + \lambda_2(1+r_{t+1}) + \lambda_2 \tau_o \omega_{t+1} l_{t+1} \frac{\partial h_{t+1}}{\partial s_t} = 0 \quad \text{(式16)}$$

**对$e_t$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial e_t} = -\lambda_1 n_t + \delta v'(h_{t+1})\frac{\partial h_{t+1}}{\partial e_t} + \lambda_2 \tau_o \omega_{t+1} l_{t+1} \frac{\partial h_{t+1}}{\partial e_t} = 0 \quad \text{(式17)}$$

**对$\tau_c$求偏导：**
$$\frac{\partial \mathcal{L}}{\partial \tau_c} = \lambda_1(1-\tau-\tau_o)\omega_t h_t \frac{\partial l_t}{\partial \tau_c} + \delta v'(h_{t+1})\frac{\partial h_{t+1}}{\partial \tau_c} = 0 \quad \text{(式18)}$$

---

### （三）欧拉方程推导

由式(13)和式(14)：
$$c_t^{-\sigma} = \beta c_{t+1}^{-\sigma}$$

$$\Rightarrow \frac{c_{t+1}}{c_t} = \beta^{-1/\sigma} \quad \text{(式19)}$$

由式(13)和式(16)，注意到在简化情形下（忽略$h_{t+1}$对$s_t$的间接影响）：
$$c_t^{-\sigma} = \beta(1+r_{t+1}) c_{t+1}^{-\sigma}$$

结合式(19)，得到标准的欧拉方程：
$$1 = \beta(1+r_{t+1}) \left(\frac{c_{t+1}}{c_t}\right)^{-\sigma} = \beta(1+r_{t+1}) \beta^{\sigma/\sigma} = (1+r_{t+1})\beta \quad \text{(式20)}$$

这意味着在最优状态下：
$$1+r_{t+1} = \frac{1}{\beta} \quad \text{(式21)}$$

---

## 三、储蓄率的解析解

### （一）简化变量定义

定义t期子代的**总转移支出**：
$$TR_t = \tau_o \omega_t h_t l_t + n_t e_t - b_t \quad \text{(式22)}$$

这包括：赡养支出、抚育支出，扣除继承收入。

定义**可支配收入**：
$$Y_t^d = (1-\tau)\omega_t h_t l_t + b_t \quad \text{(式23)}$$

则预算约束改写为：
$$Y_t^d = c_t + s_t + TR_t \quad \text{(式24)}$$

同理，t+1期总转移为：
$$TR_{t+1} = \tau_o \omega_{t+1} h_{t+1} l_{t+1} + n_{t+1} e_{t+1} - b_{t+1} \quad \text{(式25)}$$

t+1期可支配收入：
$$Y_{t+1}^d = (1-\tau)\omega_{t+1} h_{t+1} l_{t+1} + b_{t+1} \quad \text{(式26)}$$

---

### （二）跨期预算约束

从式(24)：$s_t = Y_t^d - c_t - TR_t$

t+1期预算约束：
$$c_{t+1} = (1+r_{t+1})s_t + Y_{t+1}^d - TR_{t+1}$$

代入$s_t$：
$$c_{t+1} = (1+r_{t+1})(Y_t^d - c_t - TR_t) + Y_{t+1}^d - TR_{t+1}$$

整理得跨期预算约束：
$$c_t + \frac{c_{t+1}}{1+r_{t+1}} = Y_t^d + \frac{Y_{t+1}^d}{1+r_{t+1}} - TR_t - \frac{TR_{t+1}}{1+r_{t+1}} \quad \text{(式27)}$$

---

### （三）消费函数

在CRRA效用下，最优消费满足：
$$c_{t+1} = \beta^{1/\sigma}(1+r_{t+1})^{1/\sigma} c_t$$

若$\sigma = 1$（对数效用），则：
$$c_{t+1} = \beta(1+r_{t+1}) c_t \quad \text{(式28)}$$

代入跨期预算约束(27)：
$$c_t + \frac{\beta(1+r_{t+1}) c_t}{1+r_{t+1}} = Y_t^d + \frac{Y_{t+1}^d}{1+r_{t+1}} - TR_t - \frac{TR_{t+1}}{1+r_{t+1}}$$

$$c_t(1+\beta) = Y_t^d + \frac{Y_{t+1}^d}{1+r_{t+1}} - TR_t - \frac{TR_{t+1}}{1+r_{t+1}}$$

$$c_t = \frac{1}{1+\beta}\left[Y_t^d - TR_t + \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}\right] \quad \text{(式29)}$$

---

### （四）储蓄函数

从$s_t = Y_t^d - c_t - TR_t$，代入式(29)：

$$s_t = Y_t^d - TR_t - \frac{1}{1+\beta}\left[Y_t^d - TR_t + \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}\right]$$

$$= \left(1 - \frac{1}{1+\beta}\right)(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}$$

$$= \frac{\beta}{1+\beta}(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}} \quad \text{(式30)}$$

---

### （五）储蓄率

储蓄率定义为：
$$\rho_t = \frac{s_t}{Y_t^d}$$

为简化分析，假设稳态条件下：
- $Y_{t+1}^d = (1+g) Y_t^d$（收入增长率为$g$）
- $TR_{t+1} = (1+g) TR_t$（转移支出同比例增长）

代入式(30)：
$$s_t = \frac{\beta}{1+\beta}(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{(1+g)}{1+r_{t+1}}(Y_t^d - TR_t)$$

$$= (Y_t^d - TR_t)\left[\frac{\beta}{1+\beta} - \frac{(1+g)}{(1+\beta)(1+r_{t+1})}\right]$$

$$= (Y_t^d - TR_t) \cdot \frac{\beta(1+r_{t+1}) - (1+g)}{(1+\beta)(1+r_{t+1})} \quad \text{(式31)}$$

因此储蓄率为：
$$\rho_t = \frac{s_t}{Y_t^d} = \left(1 - \frac{TR_t}{Y_t^d}\right) \cdot \frac{\beta(1+r_{t+1}) - (1+g)}{(1+\beta)(1+r_{t+1})} \quad \text{(式32)}$$

---

### （六）储蓄率的进一步简化

在稳态均衡下，若$r_{t+1} = 1/\beta - 1$（由欧拉方程得出），且$g=0$（零增长），则：

$$\beta(1+r_{t+1}) = 1$$

储蓄率简化为：
$$\rho_t = \left(1 - \frac{TR_t}{Y_t^d}\right) \cdot \frac{1-1}{(1+\beta) \cdot 1/\beta} = 0$$

这在零增长经济中是合理的。但在一般情形下，我们保留式(32)的形式。

**更实用的表达式：**

回到式(30)，直接除以$Y_t^d$：
$$\rho_t = \frac{\beta}{1+\beta}\left(1-\frac{TR_t}{Y_t^d}\right) - \frac{1}{1+\beta}\cdot\frac{Y_{t+1}^d/Y_t^d - TR_{t+1}/Y_t^d}{1+r_{t+1}}$$

定义**转移支出比率**：$\tau_{TR,t} = TR_t/Y_t^d$，则：

$$\boxed{\rho_t = \frac{\beta(1-\tau_{TR,t})}{1+\beta} - \frac{(1+g)(1-\tau_{TR,t+1})}{(1+\beta)(1+r_{t+1})}} \quad \text{(式33)}$$

---

## 四、赡养行为的内生化

### （一）赡养支出比例的决定

赡养支出比例$\tau_o$并非外生给定，而是由家庭在考虑父代需求、社会规范和经济条件后内生决定的。

**赡养支出决定函数：**
$$\tau_o = g_1(D_t, \text{养老金替代率}, \text{父代健康}, \text{社会规范}) \quad \text{(式34)}$$

**简化的线性形式：**
$$\tau_o = \tau_0 + \mu_1 D_t + \mu_2 (1-\rho_{pen}) \quad \text{(式35)}$$

其中：
- $\tau_0$：基础赡养支出比例
- $\mu_1 > 0$：赡养比对赡养支出的影响系数
- $\rho_{pen}$：养老金替代率（$pen_t/\omega_t h_t$）
- $\mu_2 > 0$：养老金缺口对赡养支出的影响系数

**经济含义：**
1. 赡养比$D_t$越高（老年人越多），子代赡养支出比例越大
2. 养老金替代率越低，子代需要承担更多赡养责任

**偏导数：**
$$\frac{\partial \tau_o}{\partial D_t} = \mu_1 > 0 \quad \text{(式36)}$$

---

### （二）照料时间的决定

照料时间$\tau_o$取决于父代的健康状况和长期护理服务的可得性。

**照料时间决定函数：**
$$\tau_o = g_2(D_t, \text{父代健康指数}, \text{护理服务价格}) \quad \text{(式37)}$$

**简化形式：**
$$\tau_o = \tau_{o,0} + \nu_1 D_t \cdot (1-H_t) \quad \text{(式38)}$$

其中：
- $\tau_{o,0}$：基础照料时间
- $\nu_1 > 0$：照料需求系数
- $H_t \in [0,1]$：父代健康指数（越高越健康）
- $(1-H_t)$：失能程度

**经济含义：**
赡养比越高且父代健康状况越差，子代需要投入更多照料时间。

**偏导数：**
$$\frac{\partial \tau_o}{\partial D_t} = \nu_1(1-H_t) > 0 \quad \text{(式39)}$$

---

### （三）预期寿命与赡养风险

父代存活概率$p$反映预期寿命，与老龄化程度相关。

**存活概率函数：**
$$p = g_3(D_t, \text{医疗水平}, t) \quad \text{(式40)}$$

**简化形式：**
$$p = p_0 + \kappa D_t \quad \text{(式41)}$$

其中：
- $p_0$：基准存活概率
- $\kappa > 0$：老龄化与预期寿命的正相关系数

**经济含义：**
人口老龄化通常伴随预期寿命延长，子代面临更长的赡养期和更高的赡养风险。

**偏导数：**
$$\frac{\partial p}{\partial D_t} = \kappa > 0 \quad \text{(式42)}$$

---

## 五、中介效应的完整分解

### （一）储蓄率对关键变量的偏导数

从储蓄率表达式（式33），我们需要分析$\rho_t$如何受到赡养行为变量的影响。

回到储蓄函数的原始形式（式30）：
$$s_t = \frac{\beta}{1+\beta}(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}$$

其中：
- $Y_t^d = (1-\tau)\omega_t h_t l_t + b_t$
- $TR_t = \tau_o \omega_t h_t l_t + n_t e_t - b_t$
- $l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g$

---

#### 1. 储蓄率对赡养支出比例的偏导数

$$\frac{\partial \rho_t}{\partial \tau_o}$$

从$TR_t = \tau_o \omega_t h_t l_t + n_t e_t - b_t$，有：
$$\frac{\partial TR_t}{\partial \tau_o} = \omega_t h_t l_t + \tau_o \omega_t h_t \frac{\partial l_t}{\partial \tau_o}$$

从$l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g$，有：
$$\frac{\partial l_t}{\partial \tau_o} = -1$$

因此：
$$\frac{\partial TR_t}{\partial \tau_o} = \omega_t h_t l_t - \tau_o \omega_t h_t = \omega_t h_t(l_t - \tau_o) \quad \text{(式43)}$$

从$Y_t^d = (1-\tau)\omega_t h_t l_t + b_t$，有：
$$\frac{\partial Y_t^d}{\partial \tau_o} = (1-\tau)\omega_t h_t \frac{\partial l_t}{\partial \tau_o} = -(1-\tau)\omega_t h_t \quad \text{(式44)}$$

储蓄对$\tau_o$的偏导数：
$$\frac{\partial s_t}{\partial \tau_o} = \frac{\beta}{1+\beta}\left(\frac{\partial Y_t^d}{\partial \tau_o} - \frac{\partial TR_t}{\partial \tau_o}\right)$$

$$= \frac{\beta}{1+\beta}\left[-(1-\tau)\omega_t h_t - \omega_t h_t(l_t - \tau_o)\right]$$

$$= -\frac{\beta \omega_t h_t}{1+\beta}\left[(1-\tau) + l_t - \tau_o\right] \quad \text{(式45)}$$

由于$l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g < 1$，且$\tau_o$通常较小，因此：
$$(1-\tau) + l_t - \tau_o > 0$$

**结论：**
$$\boxed{\frac{\partial s_t}{\partial \tau_o} < 0} \quad \text{(式46)}$$

**经济含义：** 赡养支出比例增加会通过两个渠道降低储蓄：
1. 直接减少可支配收入（增加当期转移支出）
2. 减少劳动时间，进一步降低收入

---

#### 2. 储蓄率对照料时间的偏导数

$$\frac{\partial \rho_t}{\partial \tau_o}$$

从$l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g$，有：
$$\frac{\partial l_t}{\partial \tau_o} = -1$$

因此：
$$\frac{\partial Y_t^d}{\partial \tau_o} = (1-\tau)\omega_t h_t \frac{\partial l_t}{\partial \tau_o} = -(1-\tau)\omega_t h_t \quad \text{(式47)}$$

照料时间不直接影响$TR_t$中的赡养支出部分（因为$\tau_o$是比例，不是绝对值），但会通过减少劳动收入间接影响：

$$\frac{\partial TR_t}{\partial \tau_o} = \tau_o \omega_t h_t \frac{\partial l_t}{\partial \tau_o} + n_t \frac{\partial e_t}{\partial \tau_o}$$

假设教育投资$e_t$对照料时间不敏感（一阶近似），则：
$$\frac{\partial TR_t}{\partial \tau_o} = -\tau_o \omega_t h_t \quad \text{(式48)}$$

储蓄对$\tau_o$的偏导数：
$$\frac{\partial s_t}{\partial \tau_o} = \frac{\beta}{1+\beta}\left(\frac{\partial Y_t^d}{\partial \tau_o} - \frac{\partial TR_t}{\partial \tau_o}\right)$$

$$= \frac{\beta}{1+\beta}\left[-(1-\tau)\omega_t h_t + \tau_o \omega_t h_t\right]$$

$$= \frac{\beta \omega_t h_t}{1+\beta}\left[\tau_o - (1-\tau)\right] \quad \text{(式49)}$$

由于通常$\tau_o \ll (1-\tau)$（赡养支出比例远小于税后收入比例），因此：
$$\tau_o - (1-\tau) < 0$$

但考虑到**未来收入不确定性增加**，照料时间延长会触发**预防性储蓄动机**。修正后的偏导数应包含预防性储蓄项：

$$\frac{\partial s_t}{\partial \tau_o} = \frac{\beta \omega_t h_t}{1+\beta}\left[\tau_o - (1-\tau)\right] + \underbrace{\theta_{precautionary} \cdot \sigma_y^2}_{\text{预防性储蓄}}$$

其中$\sigma_y^2$为未来收入方差，$\theta_{precautionary} > 0$。

**结论：**
$$\boxed{\frac{\partial s_t}{\partial \tau_o} > 0 \text{ （当预防性储蓄动机主导时）}} \quad \text{(式50)}$$

**经济含义：** 照料时间延长减少当期收入，增加未来收入不确定性，激发预防性储蓄，总效应为正。

---

#### 3. 储蓄率对存活概率的偏导数

$$\frac{\partial \rho_t}{\partial p}$$

存活概率$p$影响t+1期父代的赡养需求和预期赡养支出。

从父代预算约束（式6）：
$$c_{t+1}^o + n_t m_{g,t+1} = (1+r_{t+1})s_t + \tau_o \omega_{t+1} h_{t+1} l_{t+1} + pen_t$$

存活概率上升意味着子代在t+1期需要赡养父代的概率增加，因此：
$$\frac{\partial TR_{t+1}}{\partial p} = \frac{\partial[\tau_o \omega_{t+1} h_{t+1} l_{t+1}]}{\partial p} > 0 \quad \text{(式51)}$$

从储蓄函数（式30）：
$$s_t = \frac{\beta}{1+\beta}(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}$$

对$p$求偏导：
$$\frac{\partial s_t}{\partial p} = -\frac{1}{1+\beta} \cdot \frac{1}{1+r_{t+1}} \left(-\frac{\partial TR_{t+1}}{\partial p}\right)$$

$$= \frac{1}{(1+\beta)(1+r_{t+1})} \frac{\partial TR_{t+1}}{\partial p} > 0 \quad \text{(式52)}$$

**结论：**
$$\boxed{\frac{\partial s_t}{\partial p} > 0} \quad \text{(式53)}$$

**经济含义：** 父代预期寿命延长（存活概率上升）意味着子代面临更长的赡养期和更高的预期赡养支出，基于预防性储蓄动机，子代会增加当期储蓄。

---

### （二）老龄化对储蓄率的总效应分解

根据链式法则，老龄化（赡养比$D_t$上升）对储蓄率的总效应可分解为：

$$\frac{d\rho_t}{dD_t} = \underbrace{\frac{\partial \rho_t}{\partial D_t}\bigg|_{\tau_o, \tau_o, p}}_{\text{直接效应}} + \underbrace{\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t}}_{\text{赡养支出中介}} + \underbrace{\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t}}_{\text{照料时间中介}} + \underbrace{\frac{\partial \rho_t}{\partial p}\frac{\partial p}{\partial D_t}}_{\text{赡养风险中介}} \quad \text{(式54)}$$

---

#### 1. 直接效应

直接效应是指在赡养行为不变的情况下，赡养比通过其他渠道（如遗产、家庭规模效应）影响储蓄率。

从储蓄率表达式，$D_t$会影响：
- 继承收入$b_t$（通过父代遗产分配）
- 人均资本积累（宏观效应）

**一阶近似：**
$$\frac{\partial \rho_t}{\partial D_t}\bigg|_{\tau_o, \tau_o, p} = \frac{\partial \rho_t}{\partial b_t} \cdot \frac{\partial b_t}{\partial D_t}$$

从式(5)：$b_t = (1+r_t)s_{t-1}\frac{1-p}{n_{t-1}} + D_t m_{g,t}$，有：
$$\frac{\partial b_t}{\partial D_t} = m_{g,t} > 0 \quad \text{(式55)}$$

继承收入增加会提高可支配收入，但对储蓄的影响取决于边际储蓄倾向。在标准生命周期模型中，意外之财的边际储蓄倾向接近$\beta/(1+\beta)$，因此：

$$\frac{\partial \rho_t}{\partial D_t}\bigg|_{\tau_o, \tau_o, p} \approx \frac{\beta}{(1+\beta)Y_t^d} m_{g,t} > 0 \quad \text{(式56)}$$

但这一效应通常较小。

---

#### 2. 赡养支出中介效应

从式(36)和式(46)：
$$\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t} = \left(-\frac{\beta \omega_t h_t}{1+\beta}\left[(1-\tau) + l_t - \tau_o\right]\right) \cdot \mu_1$$

$$= -\frac{\beta \mu_1 \omega_t h_t}{1+\beta}\left[(1-\tau) + l_t - \tau_o\right] < 0 \quad \text{(式57)}$$

**经济含义：** 赡养比上升→赡养支出增加→当期收入减少→储蓄率下降（负向中介效应）

---

#### 3. 照料时间中介效应

从式(39)和式(50)：
$$\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t} = \left[\frac{\beta \omega_t h_t}{1+\beta}\left[\tau_o - (1-\tau)\right] + \theta_{precautionary} \cdot \sigma_y^2\right] \cdot \nu_1(1-H_t)$$

在预防性储蓄动机主导下：
$$\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t} \approx \theta_{precautionary} \cdot \sigma_y^2 \cdot \nu_1(1-H_t) > 0 \quad \text{(式58)}$$

**经济含义：** 赡养比上升→照料时间增加→劳动收入不确定性增加→预防性储蓄上升（正向中介效应）

---

#### 4. 赡养风险中介效应

从式(42)和式(53)：
$$\frac{\partial \rho_t}{\partial p}\frac{\partial p}{\partial D_t} = \frac{1}{(1+\beta)(1+r_{t+1})} \frac{\partial TR_{t+1}}{\partial p} \cdot \kappa > 0 \quad \text{(式59)}$$

**经济含义：** 赡养比上升→预期寿命延长→赡养期延长→预防性储蓄上升（正向中介效应）

---

### （三）总效应的符号判断

将式(56)、(57)、(58)、(59)代入式(54)：

$$\frac{d\rho_t}{dD_t} = \underbrace{(+)}_{\text{直接效应}} + \underbrace{(-)}_{\text{赡养支出中介}} + \underbrace{(+)}_{\text{照料时间中介}} + \underbrace{(+)}_{\text{赡养风险中介}}$$

$$= \underbrace{\frac{\beta m_{g,t}}{(1+\beta)Y_t^d}}_{>0} \underbrace{- \frac{\beta \mu_1 \omega_t h_t[(1-\tau) + l_t - \tau_o]}{1+\beta}}_{<0} + \underbrace{\theta_{precautionary} \sigma_y^2 \nu_1(1-H_t)}_{>0} + \underbrace{\frac{\kappa}{(1+\beta)(1+r_{t+1})} \frac{\partial TR_{t+1}}{\partial p}}_{>0} \quad \text{(式60)}$$

**总效应的符号取决于：**
1. **赡养支出效应的大小**（$\mu_1$，$\tau_o$）
2. **预防性储蓄动机的强度**（$\theta_{precautionary}$，$\sigma_y^2$）
3. **预期寿命变化幅度**（$\kappa$）

---

## 六、少子化对储蓄率的影响机制

### （一）少子化的定义与测度

**少子化**（Fertility Decline）是指生育率$n_t$持续下降，导致儿童抚养比$C_t = n_t$降低的人口学现象。

**关键变量：**
- **生育率**：$n_t$（每个子代个体生育的孩子数量）
- **儿童抚养比**：$C_t = n_t$（孙代与子代的人口比例）
- **抚育支出**：$n_t e_t$（对孙代的教育投资总额）
- **抚育时间**：$n_t \tau_c$（对孙代的时间投入总量）

**少子化的经济效应：**
$$n_t \downarrow \Rightarrow C_t \downarrow \Rightarrow \begin{cases}
\text{抚育支出下降：} n_t e_t \downarrow \\
\text{抚育时间减少：} n_t \tau_c \downarrow \\
\text{劳动时间增加：} l_t \uparrow \\
\text{可支配收入变化} \\
\text{储蓄率变化}
\end{cases}$$

---

### （二）少子化对储蓄率的直接效应

从储蓄率表达式（式33）和转移支出定义（式22），我们分析生育率$n_t$对储蓄率的直接影响。

#### 1. 通过抚育支出渠道

从$TR_t = \tau_o \omega_t h_t l_t + n_t e_t - b_t$，生育率影响抚育支出：

$$\frac{\partial TR_t}{\partial n_t} = e_t + n_t \frac{\partial e_t}{\partial n_t} + \tau_o \omega_t h_t \frac{\partial l_t}{\partial n_t} \quad \text{(式61)}$$

从时间约束$l_t = 1 - n_t \tau_c - \tau_o + \xi \tau_g$（式3），有：

$$\frac{\partial l_t}{\partial n_t} = -\tau_c - n_t \frac{\partial \tau_c}{\partial n_t} \quad \text{(式62)}$$

假设边际调整较小（$\frac{\partial e_t}{\partial n_t} \approx 0$，$\frac{\partial \tau_c}{\partial n_t} \approx 0$），则：

$$\frac{\partial TR_t}{\partial n_t} \approx e_t - \tau_o \omega_t h_t \tau_c \quad \text{(式63)}$$

由于$e_t > 0$且$\tau_o \omega_t h_t \tau_c$通常较小（照料时间占比不高），因此：

$$\boxed{\frac{\partial TR_t}{\partial n_t} > 0} \quad \text{(式64)}$$

**经济含义：** 生育率上升→抚育支出增加→转移支出增加。

---

#### 2. 通过劳动时间渠道

从可支配收入$Y_t^d = (1-\tau)\omega_t h_t l_t + b_t$（式23），有：

$$\frac{\partial Y_t^d}{\partial n_t} = (1-\tau)\omega_t h_t \frac{\partial l_t}{\partial n_t} = -(1-\tau)\omega_t h_t \tau_c < 0 \quad \text{(式65)}$$

**经济含义：** 生育率上升→抚育时间增加→劳动时间减少→劳动收入下降→可支配收入下降。

---

#### 3. 对储蓄的综合影响

从储蓄函数（式30）：

$$s_t = \frac{\beta}{1+\beta}(Y_t^d - TR_t) - \frac{1}{1+\beta} \cdot \frac{Y_{t+1}^d - TR_{t+1}}{1+r_{t+1}}$$

对$n_t$求偏导：

$$\frac{\partial s_t}{\partial n_t} = \frac{\beta}{1+\beta}\left(\frac{\partial Y_t^d}{\partial n_t} - \frac{\partial TR_t}{\partial n_t}\right)$$

代入式(63)和(65)：

$$\frac{\partial s_t}{\partial n_t} = \frac{\beta}{1+\beta}\left[-(1-\tau)\omega_t h_t \tau_c - e_t + \tau_o \omega_t h_t \tau_c\right]$$

$$= -\frac{\beta}{1+\beta}\left[(1-\tau)\omega_t h_t \tau_c + e_t - \tau_o \omega_t h_t \tau_c\right]$$

$$= -\frac{\beta}{1+\beta}\left[(1-\tau-\tau_o)\omega_t h_t \tau_c + e_t\right] \quad \text{(式66)}$$

由于$(1-\tau-\tau_o) > 0$且$e_t > 0$，因此：

$$\boxed{\frac{\partial s_t}{\partial n_t} < 0} \quad \text{(式67)}$$

**结论：** 生育率上升导致储蓄下降，反之，**少子化（$n_t \downarrow$）导致储蓄率上升**。

---

#### 4. 储蓄率的弹性分析

为了量化少子化对储蓄率的影响，我们计算储蓄率对生育率的弹性。

从$\rho_t = s_t / Y_t^d$，有：

$$\frac{\partial \rho_t}{\partial n_t} = \frac{1}{Y_t^d}\frac{\partial s_t}{\partial n_t} - \frac{s_t}{(Y_t^d)^2}\frac{\partial Y_t^d}{\partial n_t}$$

$$= \frac{1}{Y_t^d}\left[\frac{\partial s_t}{\partial n_t} - \rho_t \frac{\partial Y_t^d}{\partial n_t}\right] \quad \text{(式68)}$$

代入式(66)和(65)：

$$\frac{\partial \rho_t}{\partial n_t} = \frac{1}{Y_t^d}\left[-\frac{\beta}{1+\beta}\left[(1-\tau-\tau_o)\omega_t h_t \tau_c + e_t\right] + \rho_t (1-\tau)\omega_t h_t \tau_c\right]$$

由于$\rho_t$通常较小（典型值0.01-0.02），第二项可忽略，因此：

$$\boxed{\frac{\partial \rho_t}{\partial n_t} < 0} \quad \text{(式69)}$$

**弹性表达式：**

$$\varepsilon_{\rho,n} = \frac{\partial \rho_t}{\partial n_t} \cdot \frac{n_t}{\rho_t} = -\frac{\beta n_t}{(1+\beta)\rho_t Y_t^d}\left[(1-\tau-\tau_o)\omega_t h_t \tau_c + e_t\right] < 0 \quad \text{(式70)}$$

**政策含义：** 生育率每下降1%，储蓄率将上升$|\varepsilon_{\rho,n}|$%。

---

### （三）少子化对储蓄率的中介效应分解

少子化不仅通过直接渠道（抚育支出、劳动时间）影响储蓄率，还会通过影响人口结构产生**间接效应**。

#### 中介效应1：未来老龄化预期渠道

**机制链条：**

$$n_t \downarrow \Rightarrow D_{t+k} = \frac{1}{n_t} \uparrow \Rightarrow \mathbb{E}[\text{未来赡养负担}] \uparrow \Rightarrow \text{预防性储蓄} \uparrow$$

生育率下降会导致未来老年抚养比上升。理性预期下，子代会预期到未来需要赡养更多父代，从而增加当期储蓄。

**数学表达：**

未来赡养比：$D_{t+1} = \frac{N_t}{N_{t+1}} = \frac{1}{n_t}$

$$\frac{\partial D_{t+1}}{\partial n_t} = -\frac{1}{n_t^2} < 0 \quad \text{(式71)}$$

从式(54)的中介效应分解，未来老龄化会通过赡养支出、照料时间和赡养风险三个渠道影响未来储蓄需求。当期储蓄会对未来预期作出反应：

$$\frac{\partial s_t}{\partial n_t}\bigg|_{\text{预期渠道}} = \frac{\partial s_t}{\partial \mathbb{E}[TR_{t+1}]} \cdot \frac{\partial \mathbb{E}[TR_{t+1}]}{\partial D_{t+1}} \cdot \frac{\partial D_{t+1}}{\partial n_t} \quad \text{(式72)}$$

从储蓄函数（式30）：

$$\frac{\partial s_t}{\partial \mathbb{E}[TR_{t+1}]} = -\frac{1}{1+\beta} \cdot \frac{1}{1+r_{t+1}} \cdot \left(-1\right) = \frac{1}{(1+\beta)(1+r_{t+1})} > 0 \quad \text{(式73)}$$

且从式(35)：

$$\frac{\partial \mathbb{E}[TR_{t+1}]}{\partial D_{t+1}} = \frac{\partial \tau_o}{\partial D_{t+1}} \cdot \omega_{t+1} h_{t+1} l_{t+1} = \mu_1 \omega_{t+1} h_{t+1} l_{t+1} > 0 \quad \text{(式74)}$$

因此：

$$\frac{\partial s_t}{\partial n_t}\bigg|_{\text{预期渠道}} = \frac{\mu_1 \omega_{t+1} h_{t+1} l_{t+1}}{(1+\beta)(1+r_{t+1})} \cdot \left(-\frac{1}{n_t^2}\right) < 0 \quad \text{(式75)}$$

**结论：** 生育率下降→未来老龄化加剧→预期赡养负担增加→当期储蓄增加（**正向中介效应**）。

---

#### 中介效应2：当期赡养负担缓解渠道

**机制链条：**

$$n_t \downarrow \Rightarrow \text{家庭规模} \downarrow \Rightarrow \text{人均可支配资源} \uparrow \Rightarrow \text{储蓄能力} \uparrow$$

少子化使得家庭规模缩小，人均可支配资源增加。在固定的赡养比$D_t$下，较小的家庭规模意味着更高的人均收入和更强的储蓄能力。

**数学表达：**

定义**家庭总人口**：$N_{family} = 1 + n_t + D_t$（子代1人+孙代$n_t$人+父代$D_t$人）

人均可支配收入：

$$y_t^{per\ capita} = \frac{Y_t^d - TR_t}{N_{family}} = \frac{Y_t^d - TR_t}{1 + n_t + D_t} \quad \text{(式76)}$$

对$n_t$求偏导：

$$\frac{\partial y_t^{per\ capita}}{\partial n_t} = \frac{1}{(1+n_t+D_t)^2}\left[(1+n_t+D_t)\left(\frac{\partial Y_t^d}{\partial n_t} - \frac{\partial TR_t}{\partial n_t}\right) - (Y_t^d - TR_t)\right]$$

由于$\frac{\partial Y_t^d}{\partial n_t} < 0$且$\frac{\partial TR_t}{\partial n_t} > 0$，第一项为负。同时$(Y_t^d - TR_t) > 0$，第二项也为负。因此：

$$\boxed{\frac{\partial y_t^{per\ capita}}{\partial n_t} < 0} \quad \text{(式77)}$$

**结论：** 生育率上升→人均可支配收入下降；反之，**少子化提高人均可支配收入，增强储蓄能力**（**正向效应**）。

---

#### 中介效应3：人力资本投资集中渠道

**机制链条（质量-数量权衡）：**

$$n_t \downarrow \Rightarrow \text{每孩教育投资} \uparrow \Rightarrow h_{t+1} \uparrow \Rightarrow \omega_{t+1} h_{t+1} \uparrow \Rightarrow \text{未来收入} \uparrow$$

少子化使得家庭可以将有限的教育资源集中投资于更少的孩子，提高单个孩子的人力资本水平（Becker的质量-数量替代理论）。

**数学表达：**

假设家庭教育预算固定为$E_t$，则单个孩子的教育投资为：

$$e_t = \frac{E_t}{n_t} \quad \Rightarrow \quad \frac{\partial e_t}{\partial n_t} = -\frac{E_t}{n_t^2} < 0 \quad \text{(式78)}$$

从人力资本积累函数（式1）：

$$h_{t+1} = A \cdot h_t^\alpha \cdot e_t^\beta \cdot (\theta_c \tau_c + \theta_g \tau_g)^\gamma$$

对$n_t$求偏导：

$$\frac{\partial h_{t+1}}{\partial n_t} = A \cdot h_t^\alpha \cdot \beta \cdot e_t^{\beta-1} \cdot \frac{\partial e_t}{\partial n_t} \cdot (\theta_c \tau_c + \theta_g \tau_g)^\gamma$$

$$= \beta \cdot h_{t+1} \cdot \frac{1}{e_t} \cdot \left(-\frac{E_t}{n_t^2}\right) < 0 \quad \text{(式79)}$$

**结论：** 生育率下降→单孩教育投资增加→未来人力资本提升→未来收入增加→储蓄动机变化（**长期正向效应**）。

---

### （四）少子化对储蓄率的总效应分解

综合以上分析，少子化（$n_t \downarrow$）对储蓄率的总效应可分解为：

$$\frac{d\rho_t}{dn_t} = \underbrace{\frac{\partial \rho_t}{\partial n_t}\bigg|_{\text{直接}}}_{\text{直接效应}} + \underbrace{\frac{\partial \rho_t}{\partial D_{t+1}}\frac{\partial D_{t+1}}{\partial n_t}}_{\text{未来老龄化中介}} + \underbrace{\frac{\partial \rho_t}{\partial y_t^{per\ capita}}\frac{\partial y_t^{per\ capita}}{\partial n_t}}_{\text{人均资源中介}} + \underbrace{\frac{\partial \rho_t}{\partial h_{t+1}}\frac{\partial h_{t+1}}{\partial n_t}}_{\text{人力资本中介}} \quad \text{(式80)}$$

---

#### 各渠道效应的符号判断

| **渠道** | **偏导数** | **符号** | **经济含义** |
|---------|----------|---------|------------|
| **直接效应** | $\frac{\partial \rho_t}{\partial n_t}$ | **(-)** | 生育率下降→抚育支出减少→储蓄增加 |
| **未来老龄化中介** | $\frac{\partial \rho_t}{\partial D_{t+1}}\frac{\partial D_{t+1}}{\partial n_t}$ | **(+)×(-)=(-)** | 生育率下降→未来老龄化→预防性储蓄增加 |
| **人均资源中介** | $\frac{\partial \rho_t}{\partial y_t^{per\ capita}}\frac{\partial y_t^{per\ capita}}{\partial n_t}$ | **(+)×(-)=(-)** | 生育率下降→人均收入增加→储蓄能力提升 |
| **人力资本中介** | $\frac{\partial \rho_t}{\partial h_{t+1}}\frac{\partial h_{t+1}}{\partial n_t}$ | **(+)×(-)=(-)** | 生育率下降→教育质量提升→未来收入增加 |

**总效应：**

$$\boxed{\frac{d\rho_t}{dn_t} = \underbrace{(-)}_{\text{直接}} + \underbrace{(-)}_{\text{老龄化中介}} + \underbrace{(-)}_{\text{人均资源中介}} + \underbrace{(-)}_{\text{人力资本中介}} < 0} \quad \text{(式81)}$$

**结论：** 所有渠道均指向同一方向——**少子化（$n_t \downarrow$）显著提升储蓄率（$\rho_t \uparrow$）**。

---

### （五）少子化与老龄化的交互效应

在现实中，少子化与老龄化往往同时发生。我们需要分析两者的**交互作用**。

#### 联合效应分解

定义人口结构综合指标：

$$\Omega_t = \frac{D_t}{C_t} = \frac{D_t}{n_t} = \frac{1}{n_t \cdot n_{t-1}} \quad \text{(式82)}$$

$\Omega_t$越大，表示老年人口相对儿童人口越多（老龄化程度越深）。

对储蓄率的影响：

$$\frac{d\rho_t}{d\Omega_t} = \frac{\partial \rho_t}{\partial D_t}\frac{\partial D_t}{\partial \Omega_t} + \frac{\partial \rho_t}{\partial n_t}\frac{\partial n_t}{\partial \Omega_t}$$

由于$\Omega_t = D_t/n_t$，有：

$$\frac{\partial D_t}{\partial \Omega_t} = n_t, \quad \frac{\partial n_t}{\partial \Omega_t} = -\frac{D_t}{\Omega_t^2} \cdot \frac{\partial \Omega_t}{\partial n_t} = -\frac{D_t}{n_t}$$

代入得：

$$\frac{d\rho_t}{d\Omega_t} = n_t \frac{\partial \rho_t}{\partial D_t} - \frac{D_t}{n_t} \frac{\partial \rho_t}{\partial n_t} \quad \text{(式83)}$$

**符号分析：**

从式(60)，$\frac{\partial \rho_t}{\partial D_t} \gtreqless 0$（取决于中介效应的相对大小）

从式(69)，$\frac{\partial \rho_t}{\partial n_t} < 0$

因此第二项$-\frac{D_t}{n_t} \frac{\partial \rho_t}{\partial n_t} > 0$（**少子化效应为正**）

**结论：**

- 当老龄化效应为正（$\frac{\partial \rho_t}{\partial D_t} > 0$）时，**少子老龄化共同推高储蓄率**
- 当老龄化效应为负（$\frac{\partial \rho_t}{\partial D_t} < 0$）时，两者效应相反，净效应取决于$\frac{n_t \frac{\partial \rho_t}{\partial D_t}}{D_t \frac{\partial \rho_t}{\partial n_t}}$的相对大小

**实证含义：**
在预防性储蓄动机主导的经济体中（如中国、日本），少子化与老龄化的**叠加效应**会显著提升储蓄率，形成"双重储蓄压力"。

---

### （六）数值模拟的理论预期

基于以上理论推导，我们对少子化情景的数值模拟应观察到：

**预期结果1：储蓄率上升轨迹**
- 当$n_t$从1.5下降至0.5时，$\rho_t$应显著上升
- 上升幅度取决于式(81)各项的参数值

**预期结果2：分阶段效应**
- **短期**（0-15年）：直接效应主导（抚育支出下降）
- **中期**（15-30年）：老龄化预期效应开始显现
- **长期**（30-50年）：人力资本效应逐步体现

**预期结果3：中介效应占比**
- 直接效应（抚育支出下降）：贡献约40-50%
- 未来老龄化预期中介：贡献约30-40%
- 人均资源和人力资本中介：贡献约10-20%

**预期结果4：与老龄化的对比**
- 少子化对储蓄率的影响（$\frac{d\rho_t}{dn_t}$）应强于单纯老龄化（$\frac{d\rho_t}{dD_t}$）
- 因为少子化同时通过直接和间接渠道作用，且方向一致

---

## 七、理论推论

基于以上推导，我们得出以下理论推论：

### 推论1：人口结构与储蓄率（修订）

**老年赡养比与儿童抚养比对家庭储蓄率的影响方向相反。**

1. **老年赡养比的影响**（$D_t = 1/n_{t-1}$）：
   - 赡养比提高将促使家庭储蓄率上升（通过预防性储蓄渠道）
   - 但也会通过增加赡养支出降低储蓄率（负向中介效应）
   - 净效应取决于预防性储蓄动机与当期赡养负担的相对大小

2. **儿童抚养比的影响**（$C_t = n_t$）：
   - 抚养比上升（生育率上升）会降低储蓄率
   - 通过增加抚育支出和减少劳动时间两个渠道
   - 反之，**少子化（抚养比下降）会提升储蓄率**

3. **少子老龄化的叠加效应**：
   - 少子化：$n_t \downarrow \Rightarrow C_t \downarrow \Rightarrow \rho_t \uparrow$
   - 老龄化：$n_{t-1} \downarrow \Rightarrow D_t \uparrow \Rightarrow \rho_t \uparrow$（在预防性储蓄主导下）
   - **综合效应**：少子老龄化共同推高储蓄率，形成"双重储蓄压力"

**数学表达：**
$$\frac{\partial \rho_t}{\partial D_t} \gtreqless 0 \text{（取决于中介效应）}, \quad \boxed{\frac{\partial \rho_t}{\partial C_t} < 0 \text{ 或 } \frac{\partial \rho_t}{\partial n_t} < 0}$$

**核心结论**：少子化（$C_t \downarrow$或$n_t \downarrow$）**显著提升储蓄率**，效应明确且强烈，所有渠道（直接效应+三个中介效应）均指向同一方向

---

### 推论2：赡养行为与储蓄率

**少子老龄化背景下，子代赡养支出的增加会降低家庭储蓄率，而子代对父代照料时间的延长和预期赡养风险增大则会提升家庭储蓄水平。**

**数学表达：**
$$\frac{\partial \rho_t}{\partial \tau_o} < 0, \quad \frac{\partial \rho_t}{\partial \tau_o} > 0, \quad \frac{\partial \rho_t}{\partial p} > 0$$

---

### 推论3（新增）：中介效应推论

**人口老龄化（赡养比上升）通过三个中介渠道影响家庭储蓄率：**

1. **赡养支出渠道**（$\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t} < 0$）：
   赡养比上升→赡养支出增加→当期可支配收入下降→储蓄率下降

2. **照料时间渠道**（$\frac{\partial \rho_t}{\partial \tau_o}\frac{\partial \tau_o}{\partial D_t} > 0$）：
   赡养比上升→照料时间增加→劳动时间减少→未来收入不确定性增加→预防性储蓄上升

3. **赡养风险渠道**（$\frac{\partial \rho_t}{\partial p}\frac{\partial p}{\partial D_t} > 0$）：
   赡养比上升→父代预期寿命延长→赡养期延长→预防性储蓄上升

**人口老龄化对储蓄率的总效应取决于上述中介效应的综合作用，且存在异质性：**
- 当赡养支出效应主导时，老龄化降低储蓄率
- 当预防性储蓄动机（照料时间+赡养风险）主导时，老龄化提升储蓄率

**数学表达：**
$$\text{sign}\left(\frac{d\rho_t}{dD_t}\right) = \text{sign}\left[\text{直接效应} - |\text{赡养支出中介}| + \text{照料时间中介} + \text{赡养风险中介}\right]$$

---

### 推论4（新增）：少子化中介效应推论

**少子化（生育率下降）通过四个渠道影响家庭储蓄率：**

1. **直接效应-抚育支出渠道**（$\frac{\partial \rho_t}{\partial n_t} < 0$）：
   生育率下降→抚育支出和抚育时间减少→可支配收入增加+劳动时间增加→储蓄率上升

2. **未来老龄化预期中介**（$\frac{\partial \rho_t}{\partial D_{t+1}}\frac{\partial D_{t+1}}{\partial n_t} < 0$）：
   生育率下降→未来老年抚养比上升→预期赡养负担增加→预防性储蓄上升→当期储蓄率上升

3. **人均资源中介**（$\frac{\partial \rho_t}{\partial y_t^{per\ capita}}\frac{\partial y_t^{per\ capita}}{\partial n_t} < 0$）：
   生育率下降→家庭规模缩小→人均可支配资源增加→储蓄能力提升→储蓄率上升

4. **人力资本投资中介**（$\frac{\partial \rho_t}{\partial h_{t+1}}\frac{\partial h_{t+1}}{\partial n_t} < 0$）：
   生育率下降→单孩教育投资增加（质量-数量替代）→未来人力资本提升→未来收入预期增加→储蓄率上升

**少子化对储蓄率的总效应：所有渠道同向叠加**

$$\frac{d\rho_t}{dn_t} = \underbrace{(-)}_{\text{直接}} + \underbrace{(-)}_{\text{老龄化中介}} + \underbrace{(-)}_{\text{人均资源中介}} + \underbrace{(-)}_{\text{人力资本中介}} < 0$$

**核心发现：** 少子化对储蓄率的影响**明确为正**且**强于单纯老龄化**，因为所有渠道均强化主效应，不存在抵消效应。这解释了为什么低生育率国家（如日本、韩国、中国）普遍面临储蓄率过高问题。

---

## 八、政策含义

### （一）养老金制度的作用

从式(35)，养老金替代率提高（$\rho_{pen}$上升）会降低子代赡养支出比例$\tau_o$，从而：

$$\frac{d\rho_t}{d\rho_{pen}} = \frac{\partial \rho_t}{\partial \tau_o} \cdot \frac{\partial \tau_o}{\partial \rho_{pen}} = \left(-\frac{\beta \omega_t h_t}{1+\beta}[(1-\tau) + l_t - \tau_o]\right) \cdot (-\mu_2) > 0$$

**政策含义：** 提高养老金待遇可以减轻子代赡养负担，释放储蓄空间。

---

### （二）长期护理保险的作用

发展长期护理服务市场可以降低$\nu_1$（照料需求系数），从而：

$$\frac{d\rho_t}{d\nu_1} = \frac{\partial \rho_t}{\partial \tau_o} \cdot \frac{\partial \tau_o}{\partial \nu_1} = \left[\theta_{precautionary} \cdot \sigma_y^2\right] \cdot D_t(1-H_t)$$

**政策含义：** 发展长期护理保险可以减少照料时间带来的收入不确定性，降低过度预防性储蓄。

---

### （三）人力资本投资的作用

从改进的人力资本函数（式1），父代隔代抚育（$\tau_g$，$m_{g,t}$）可以：
1. 提升孙代人力资本水平
2. 释放子代劳动时间
3. 影响代际资源配置

**政策含义：** 鼓励"三代同堂"和隔代抚育可以优化家庭时间配置，提升人力资本积累效率。

---

## 八、总结



1. **明确区分子代和父代在人力资本积累中的时间投入**（式1），体现代际差异

2. **内生化赡养行为**（式35、38、41），建立老龄化→赡养行为的传导机制

3. **分解中介效应**（式54-60），揭示老龄化通过赡养支出、照料时间、赡养风险三个渠道影响储蓄率

4. **理论推论明确**（推论1-3），为实证检验提供理论基础

